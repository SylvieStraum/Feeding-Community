import React, { Component } from 'react';
import { connect } from 'react-redux';

class DriverFilter extends Component {
  // Renders the entire DriverFilter on the DOM
  render() {
    return (
      <div className="DriverFilter">
        <p>DriverFilter</p>
      </div>
    );//end return
  }//end render
}//end class

export default DriverFilter;